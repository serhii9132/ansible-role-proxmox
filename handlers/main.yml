---
- name: Reboot server
  ansible.builtin.reboot:
    connect_timeout: 5
    reboot_timeout: 300
    post_reboot_delay: 30
    test_command: whoami

- name: Update grub
  ansible.builtin.command: update-grub
  failed_when: false
  changed_when: true
