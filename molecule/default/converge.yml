---
- name: Converge
  hosts: all
  gather_facts: true
  tasks:
    - name: "Include ansible-role-proxmox"
      ansible.builtin.include_role:
        name: "../../../ansible-role-proxmox/" # noqa: role-name
